---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: anodiam-gateway-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anodiam-gateway-service
  template:
    metadata:
      labels:
        app: anodiam-gateway-service
    spec:
      containers:
        - name: anodiam-gateway-service
          image: APP_IMAGE
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "gcp"